<Page x:Class="LostAndFound3.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent"
      Title="MainPage">
    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,0,12,0"/>
            <Setter Property="Background" Value="#F8FAFD"/>
            <Setter Property="BorderBrush" Value="#D5DFED"/>
            <Setter Property="BorderThickness" Value="1.5"/>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Background" Value="#F8FAFD"/>
            <Setter Property="BorderBrush" Value="#D5DFED"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFD"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="12,10"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Выдано">
                    <Setter Property="Background" Value="#DCFCE7"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="← Назад" Style="{StaticResource SecondaryButtonStyle}" Width="110" Click="Back_Click"/>
                <Button Content="Заявки" Style="{StaticResource SecondaryButtonStyle}" Width="120" Click="OpenRequests_Click"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Width="300">
                <TextBlock Text="Поиск"
                           Margin="8,0,0,6"
                           Foreground="#64748B"
                           FontSize="12"
                           FontWeight="SemiBold"/>
                <Border Background="#F8FAFD" CornerRadius="12" BorderBrush="#D5DFED" BorderThickness="1">
                    <TextBox Height="38"
                             Margin="1"
                             Background="Transparent"
                             BorderThickness="0"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </Border>
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Margin="0,18,0,18" Background="#F8FAFD" CornerRadius="18" Padding="16" BorderBrush="#E2E8F0" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Название"
                               Margin="6,0,0,6"
                               Foreground="#64748B"
                               FontSize="12"
                               FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Name}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="Описание"
                               Margin="6,0,0,6"
                               Foreground="#64748B"
                               FontSize="12"
                               FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Description}" />
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Выдать вещь" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding GiveItemCommand}"/>
                    <Button Content="Добавить" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddCommand}"/>
                    <Button Content="Удалить" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding DeleteCommand}"/>
                    <Button Content="Обновить" Style="{StaticResource SecondaryButtonStyle}" Margin="0" Command="{Binding UpdateCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="110"/>
                <DataGridTextColumn Header="Владелец" Binding="{Binding OwnerName}" Width="180"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding OwnerPhone}" Width="170"/>
                <DataGridTextColumn Header="Почта" Binding="{Binding OwnerEmail}" Width="220"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
