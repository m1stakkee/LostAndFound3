<Window x:Class="LostAndFound3.Views.MainWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LostAndFound3.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Window.Resources>
        <Style TargetType="TextBox" x:Key="WatermarkTextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            <TextBlock Text="{TemplateBinding Tag}"
                                   Foreground="Gray"
                                   Margin="5,0,0,0"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False"
                                   Visibility="Collapsed"
                                   x:Name="Watermark"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Height="30" Margin="0,0,0,10"
             Style="{StaticResource WatermarkTextBox}"
             Tag="Поиск..."
             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="150" Margin="5" Style="{StaticResource WatermarkTextBox}" Tag="Название" Text="{Binding Name}"/>
            <TextBox Width="200" Margin="5" Style="{StaticResource WatermarkTextBox}" Tag="Описание" Text="{Binding Description}"/>

            <Button Content="Выдать вещь" Margin="5" Command="{Binding GiveItemCommand}"/>
            <Button Content="Добавить" Margin="5" Command="{Binding AddCommand}"/>
            <Button Content="Удалить" Margin="5" Command="{Binding DeleteCommand}"/>
            <Button Content="Обновить" Margin="5" Command="{Binding UpdateCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
      ItemsSource="{Binding Items}"
      SelectedItem="{Binding SelectedItem}"
      AutoGenerateColumns="False">


            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Status}" Value="Выдано">
                            <Setter Property="Background" Value="#C8F7C5"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Владелец" Binding="{Binding OwnerName}" Width="150"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding OwnerPhone}" Width="150"/>
                <DataGridTextColumn Header="Почта" Binding="{Binding OwnerEmail}" Width="200"/>
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</Window>
